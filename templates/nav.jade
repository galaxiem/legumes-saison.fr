nav.annee
	each saison, nom in getSaisons()
		h3(class="col section group #{nom}") #{saison.titre}
		div(class="saison #{nom}")
			each indexMois in saison.mois
				- var mois = getMois(indexMois)
				h4.petit-rond
					a(href="/#{mois.nom}.html") #{mois.titre.substring(0,3)}